// Must trip: the archive sentence before the window messages.
export function messages() {
  const early = 'Archive data uses no forecast model.'
  const windowMessages = [early]
  return windowMessages
}
