name: PR Checks

# `pull_request` only. A `push` trigger alongside it ran this whole workflow
# TWICE per commit: the two events name different refs (`refs/heads/<branch>`
# and `refs/pull/<n>/merge`), so the concurrency group below could not collapse
# them, and all 20 sampled runs were such a pair (issue #368). They ran in
# parallel, so nobody waited longer, but each commit burned a second set of
# runners and raced the first set to write the same cache keys. The cost of
# dropping it: a branch pushed with no pull request open gets no checks until
# one is opened. Every check branch protection requires is a `pull_request`
# check anyway.
on:
  pull_request:
    branches:
      - main

# Cancel redundant runs when new commits are pushed to the same branch
concurrency:
  group: pr-${{ github.ref }}
  cancel-in-progress: true

# Least-privilege — none of these jobs write to the repo
permissions:
  contents: read

jobs:
  # Catch TypeScript type errors and unit-test failures before they reach main
  frontend:
    name: Frontend Typecheck & Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      # No `cache: npm`. Measured on 2026-09-15: `npm ci` takes 4 s on a cold
      # cache and 4-5 s on a warm one, while restoring the cache costs 2 s in
      # `setup-node` — so the cache was net negative on time. It was also 50
      # entries and 2.80 GB, one ~56 MB copy per branch, in a repository cache
      # already over GitHub's 10 GB limit and evicting the Docker layers that
      # do pay for themselves (issue #368).
      # The Node major lives in .node-version alone, so these checks run on
      # the runtime the image builds the app with.
      - uses: actions/setup-node@v7
        with:
          node-version-file: .node-version
      - run: npm ci
        working-directory: frontend
      # src/api-schema.d.ts is generated from the committed OpenAPI snapshot,
      # and src/api-compat.ts asserts the hand-written wire types against it.
      # This fails the PR when the two disagree, which means someone changed a
      # route or a model without running `npm run generate:api`. It runs before
      # the typecheck because a stale schema fails that with assertion errors
      # that do not say what is actually out of date. The script installs the
      # generator package itself, so no separate `npm ci` step is needed.
      - run: npm run check:api
        working-directory: frontend
      - run: npx tsc --noEmit
        working-directory: frontend
      # ESLint (issue #379). It runs after the typecheck because a type error
      # usually explains a lint error, and before the tests because a bad
      # dependency array is cheaper to read here than as a failing assertion.
      # The linter installs itself: it lives in frontend/tools/eslint with its
      # own lockfile, for the TypeScript-version reason recorded there.
      - run: npm run lint
        working-directory: frontend
      - run: npm test
        working-directory: frontend

  # Catch Python style and logic issues
  lint:
    name: Python Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.14'
      # Pin ruff: its default rule set changes between releases, so an unpinned
      # install can turn a green lint red with no code change on our side.
      - run: pip install ruff==0.16.0
      - run: ruff check backend/

  # Run the backend unit suite: pure logic (aggregation, validation, sorting,
  # error mapping) plus route behavior with the upstream APIs stubbed.
  backend:
    name: Backend Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      # No `cache: pip`, and that is a measurement rather than an oversight.
      # With it, the install drops 8 s to 6 s and `setup-python` grows 2-3 s
      # restoring the 25 MB it saved, so this job lands in the same 26-29 s it
      # takes without it. The wheels are small and already close to the runner;
      # the cache was never paying for a slow download (issue #368).
      - uses: actions/setup-python@v7
        with:
          python-version: '3.14'
      - run: pip install -r requirements-dev.txt
        working-directory: backend
      - run: pytest -q
        working-directory: backend
      # openapi.json is committed so a contract change shows up as a reviewable
      # diff. This fails the PR when the app and the snapshot disagree, which
      # means someone changed a route, model, or description without running
      # `python scripts/generate_openapi.py`.
      - name: Check OpenAPI snapshot is current
        run: python scripts/generate_openapi.py --check
        working-directory: backend

  # Validate the Dockerfile builds end-to-end without pushing the image, then
  # scan the result for vulnerabilities. amd64-only on purpose: this gate
  # proves the Dockerfile assembles; the release workflow builds the arm64
  # half (emulated builds would roughly double this job for no extra signal).
  docker-build:
    # Branch protection requires a check named exactly "Docker Build" — renaming
    # this job strands PRs on a phantom "Expected" check that never reports.
    name: Docker Build
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write  # Sticky scan-result comment on PRs
    steps:
      - uses: actions/checkout@v7
      - name: Lint Dockerfile
        uses: hadolint/hadolint-action@v3.5.0
        with:
          dockerfile: Dockerfile
      - uses: docker/setup-buildx-action@v4
      - uses: docker/build-push-action@v7
        with:
          context: .
          platforms: linux/amd64
          push: false
          # Load into the runner's docker daemon so Trivy can scan it, and the
          # smoke test below can actually run it
          load: true
          tags: bluebird:pr-scan
          # A sentinel rather than a real version: the smoke test asserts this
          # exact string comes back from /api/version, which is what proves the
          # ARG -> ENV -> endpoint chain survived a Dockerfile edit.
          build-args: |
            APP_VERSION=0.0.0-ci
            APP_COMMIT=${{ github.sha }}
            APP_BUILT_AT=1970-01-01T00:00:00Z
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # ignore-unfixed everywhere: Debian/Alpine ship CVEs upstream has not
      # patched; flagging those would perma-red the pipeline over vulns nobody
      # can act on. JSON once, then jq/`trivy convert` derive the human table,
      # the PR comment, and the gate from the same scan. File-level exclusions
      # live in trivy.yaml, shared with image-scan.yml so this gate and the
      # weekly re-scan judge an image the same way.
      - name: Scan image (Trivy)
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: bluebird:pr-scan
          format: json
          output: trivy.json
          ignore-unfixed: true
          trivy-config: trivy.yaml

      - name: Count Critical/High vulnerabilities
        id: crit
        uses: ./.github/actions/trivy-crit-high
        with:
          report: trivy.json

      - name: Render scan report
        env:
          PR_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
          CRIT_HIGH: ${{ steps.crit.outputs.count }}
        run: |
          TOTAL=$(jq '[.Results[]?.Vulnerabilities[]?] | length' trivy.json)
          {
            echo "<!-- bluebird-image-scan -->"
            echo "### 🛡️ Image vulnerability scan"
            echo ""
            echo "Trivy scan of this PR's image at \`${PR_SHA}\` (linux/amd64, fixable vulnerabilities only)."
            echo ""
            if [ "$TOTAL" -eq 0 ]; then
              echo "✅ **No fixable vulnerabilities.**"
            else
              echo "⚠️ **${TOTAL} fixable vulnerabilities (${CRIT_HIGH} Critical/High; Critical/High fail this check).**"
              echo ""
              echo "<details><summary>Full report</summary>"
              echo ""
              echo '```text'
              trivy convert --format table trivy.json
              echo '```'
              echo ""
              echo "</details>"
            fi
            echo ""
            echo "<hr>"
            echo ""
            echo "_Updated for each push; unfixable OS CVEs are excluded._"
          } > scan-comment.md
          cat scan-comment.md >> "$GITHUB_STEP_SUMMARY"

      # Sticky like the preview-URL comment, but matched by the hidden marker
      # instead of `--edit-last` — both comments come from github-actions[bot],
      # so editing "the last one" would clobber whichever posted more recently.
      # Skipped for Dependabot: its pull_request runs get a read-only token,
      # so the step summary above is the only surface there.
      - name: Comment scan result (sticky, rewrites on each push)
        if: github.event_name == 'pull_request' && github.actor != 'dependabot[bot]'
        env:
          GH_TOKEN: ${{ github.token }}
          PR: ${{ github.event.pull_request.number }}
        run: |
          CID=$(gh api "repos/${GITHUB_REPOSITORY}/issues/${PR}/comments" --paginate \
            --jq '.[] | select(.body | startswith("<!-- bluebird-image-scan -->")) | .id' | head -n1)
          if [ -n "$CID" ]; then
            gh api -X PATCH "repos/${GITHUB_REPOSITORY}/issues/comments/${CID}" -F body=@scan-comment.md >/dev/null
          else
            gh api -X POST "repos/${GITHUB_REPOSITORY}/issues/${PR}/comments" -F body=@scan-comment.md >/dev/null
          fi

      # Assertions that only hold against a built image, so pytest structurally
      # cannot make them. The big one is the self-hosted Swagger UI: /docs falls
      # back to a CDN whenever `static/swagger-ui` is absent, and in a source
      # checkout it always is. Without this, retargeting the Dockerfile's COPY
      # would silently restore the third-party script surface that a strict CSP
      # (#132) depends on being gone, with every other check still green.
      - name: Smoke test the built image
        run: |
          docker run -d --rm --name smoke -p 8000:8000 bluebird:pr-scan
          for _ in $(seq 1 30); do
            curl -fsS localhost:8000/healthz >/dev/null 2>&1 && break
            sleep 1
          done

          fail=0
          check() {  # label, actual, expected
            if [ "$2" = "$3" ]; then
              echo "  ok    $1"
            else
              echo "  FAIL  $1 (want '$3', got '$2')"
              fail=1
            fi
          }

          check "build args reach /api/version" \
            "$(curl -fsS localhost:8000/api/version | jq -r .version)" "0.0.0-ci"
          check "the same version fills openapi info.version" \
            "$(curl -fsS localhost:8000/openapi.json | jq -r .info.version)" "0.0.0-ci"
          check "Swagger UI is served from the image" \
            "$(curl -s -o /dev/null -w '%{http_code}' localhost:8000/swagger-ui/swagger-ui-bundle.js)" "200"
          check "/docs references no third-party host" \
            "$(curl -fsS localhost:8000/docs | grep -cE 'jsdelivr|googleapis|tiangolo' || true)" "0"
          check "/redoc is gone" \
            "$(curl -s -o /dev/null -w '%{http_code}' localhost:8000/redoc)" "404"
          check "an unknown /api path answers as the API, not the SPA" \
            "$(curl -s -o /dev/null -w '%{content_type}' localhost:8000/api/nope)" "application/json"
          check "the SPA is still served at /" \
            "$(curl -s -o /dev/null -w '%{http_code}' localhost:8000/)" "200"

          docker stop smoke >/dev/null 2>&1 || true
          [ "$fail" -eq 0 ] || echo "::error::The built image failed its smoke test. See the failures above."
          exit $fail

      # The actual gate, after the comment so a red run still explains itself
      - name: Fail on fixable Critical/High vulnerabilities
        env:
          CRIT_HIGH: ${{ steps.crit.outputs.count }}
        run: |
          if [ "$CRIT_HIGH" -gt 0 ]; then
            echo "::error::${CRIT_HIGH} fixable Critical/High vulnerabilities in the image. See the scan report above."
            exit 1
          fi

  # The cold-load gate (issue #337). Separate from Docker Build because that
  # job's name is branch-protected and because a red Lighthouse run should say
  # "the first screen got heavier", not hide inside a scan report.
  #
  # `needs: docker-build` is what makes the second build cheap: by the time
  # this runs, every layer is in the GitHub Actions cache that job filled, so
  # the build below is a cache replay rather than a second npm ci. Running the
  # two in parallel would build the image twice for real.
  lighthouse:
    name: Lighthouse Budgets
    needs: docker-build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: docker/setup-buildx-action@v4
      - uses: docker/build-push-action@v7
        with:
          context: .
          platforms: linux/amd64
          push: false
          load: true
          tags: bluebird:lighthouse
          cache-from: type=gha

      - name: Serve the built image
        run: |
          docker run -d --rm --name lighthouse-target -p 8000:8000 bluebird:lighthouse
          for _ in $(seq 1 30); do
            curl -fsS localhost:8000/healthz >/dev/null 2>&1 && break
            sleep 1
          done
          curl -fsS localhost:8000/ >/dev/null

      # The budgets, the blocked third-party hosts, and the reasoning behind
      # both live in .github/lighthouserc.js.
      - name: Audit the first screen
        uses: treosh/lighthouse-ci-action@12.6.2
        with:
          configPath: .github/lighthouserc.js
          # The HTML reports land in the run's artifacts, so a failure can be
          # read rather than guessed at. Nothing is uploaded off GitHub.
          uploadArtifacts: true
          temporaryPublicStorage: false

  # The browser smoke suite and the axe audit (issue #412): the one check that
  # operates the app. It draws a ring, analyzes, and opens a share link against
  # the image this PR built, with every third-party host answered from fixtures
  # in frontend/e2e/fixtures.ts, so it spends no Open-Meteo quota and no
  # upstream outage can turn it red.
  #
  # Shaped like the Lighthouse job for the same reason: `needs: docker-build`
  # makes the build below a cache replay. Chromium is installed on the plain
  # runner rather than run from the Playwright image, because that image is a
  # 956 MB compressed pull on every run and Chromium alone is a fraction of it.
  # The image stays the way to run the suite locally (docs/DEVELOPMENT.md).
  browser:
    name: Browser Smoke & Axe
    needs: docker-build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: docker/setup-buildx-action@v4
      - uses: docker/build-push-action@v7
        with:
          context: .
          platforms: linux/amd64
          push: false
          load: true
          tags: bluebird:browser
          cache-from: type=gha

      - name: Serve the built image
        run: |
          docker run -d --rm --name browser-target -p 8000:8000 bluebird:browser
          for _ in $(seq 1 30); do
            curl -fsS localhost:8000/healthz >/dev/null 2>&1 && break
            sleep 1
          done
          curl -fsS localhost:8000/ >/dev/null

      - uses: actions/setup-node@v7
        with:
          node-version-file: .node-version
      - run: npm ci
        working-directory: frontend/e2e

      # Keyed on the exact Playwright version, because each release pins its
      # own Chromium build and a browser from another release will not launch.
      # The install step runs on a hit too: it skips the download and still
      # installs the system libraries, which live outside the cached folder.
      - name: Read the Playwright version
        id: pw
        run: echo "version=$(jq -r '.packages["node_modules/@playwright/test"].version' frontend/e2e/package-lock.json)" >> "$GITHUB_OUTPUT"
      - uses: actions/cache@v6
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ steps.pw.outputs.version }}
      - run: npx playwright install --with-deps chromium
        working-directory: frontend/e2e

      - run: npx playwright test
        working-directory: frontend/e2e
        env:
          BASE_URL: http://localhost:8000

      # The HTML report carries a trace and a screenshot of every failed
      # test, so a red run can be read rather than guessed at.
      - uses: actions/upload-artifact@v7
        if: failure()
        with:
          name: playwright-report
          path: frontend/e2e/playwright-report
          retention-days: 7
