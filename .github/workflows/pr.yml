name: PR Checks

on:
  push:
    branches-ignore:
      - main
  pull_request:
    branches:
      - main

# Cancel redundant runs when new commits are pushed to the same branch
concurrency:
  group: pr-${{ github.ref }}
  cancel-in-progress: true

# Least-privilege — none of these jobs write to the repo
permissions:
  contents: read

jobs:
  # Catch TypeScript type errors and unit-test failures before they reach main
  frontend:
    name: Frontend Typecheck & Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
        working-directory: frontend
      - run: npx tsc --noEmit
        working-directory: frontend
      - run: npm test
        working-directory: frontend

  # The backend and frontend test containers each mount only their own
  # subtree, so the shared weather/AQI aggregation vectors exist as two
  # committed copies. Each suite asserts its own implementation against its
  # copy; this check is what makes the two copies one contract.
  vectors:
    name: Aggregation vectors in sync
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: diff backend/tests/data/weather_vectors.json frontend/src/utils/weather_vectors.json

  # Catch Python style and logic issues
  lint:
    name: Python Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.14'
      # Pin ruff: its default rule set changes between releases, so an unpinned
      # install can turn a green lint red with no code change on our side.
      - run: pip install ruff==0.16.0
      - run: ruff check backend/

  # Run the backend unit suite: pure logic (aggregation, validation, sorting,
  # error mapping) plus route behavior with the upstream APIs stubbed.
  backend:
    name: Backend Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.14'
      - run: pip install -r requirements-dev.txt
        working-directory: backend
      - run: pytest -q
        working-directory: backend
      # openapi.json is committed so a contract change shows up as a reviewable
      # diff. This fails the PR when the app and the snapshot disagree, which
      # means someone changed a route, model, or description without running
      # `python scripts/generate_openapi.py`.
      - name: Check OpenAPI snapshot is current
        run: python scripts/generate_openapi.py --check
        working-directory: backend

  # Validate the Dockerfile builds end-to-end without pushing the image, then
  # scan the result for vulnerabilities. amd64-only on purpose: this gate
  # proves the Dockerfile assembles; the release workflow builds the arm64
  # half (emulated builds would roughly double this job for no extra signal).
  docker-build:
    # Branch protection requires a check named exactly "Docker Build" — renaming
    # this job strands PRs on a phantom "Expected" check that never reports.
    name: Docker Build
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write  # Sticky scan-result comment on PRs
    steps:
      - uses: actions/checkout@v7
      - name: Lint Dockerfile
        uses: hadolint/hadolint-action@v3.3.0
        with:
          dockerfile: Dockerfile
      - uses: docker/setup-buildx-action@v4
      - uses: docker/build-push-action@v7
        with:
          context: .
          platforms: linux/amd64
          push: false
          # Load into the runner's docker daemon so Trivy can scan it, and the
          # smoke test below can actually run it
          load: true
          tags: bluebird:pr-scan
          # A sentinel rather than a real version: the smoke test asserts this
          # exact string comes back from /api/version, which is what proves the
          # ARG -> ENV -> endpoint chain survived a Dockerfile edit.
          build-args: |
            APP_VERSION=0.0.0-ci
            APP_COMMIT=${{ github.sha }}
            APP_BUILT_AT=1970-01-01T00:00:00Z
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # ignore-unfixed everywhere: Debian/Alpine ship CVEs upstream has not
      # patched; flagging those would perma-red the pipeline over vulns nobody
      # can act on. JSON once, then jq/`trivy convert` derive the human table,
      # the PR comment, and the gate from the same scan.
      - name: Scan image (Trivy)
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: bluebird:pr-scan
          format: json
          output: trivy.json
          ignore-unfixed: true

      - name: Render scan report
        env:
          PR_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          CRIT_HIGH=$(jq '[.Results[]?.Vulnerabilities[]? | select(.Severity == "CRITICAL" or .Severity == "HIGH")] | length' trivy.json)
          TOTAL=$(jq '[.Results[]?.Vulnerabilities[]?] | length' trivy.json)
          {
            echo "<!-- bluebird-image-scan -->"
            echo "### 🛡️ Image vulnerability scan"
            echo ""
            echo "Trivy scan of this PR's image at \`${PR_SHA}\` (linux/amd64, fixable vulnerabilities only)."
            echo ""
            if [ "$TOTAL" -eq 0 ]; then
              echo "✅ **No fixable vulnerabilities.**"
            else
              echo "⚠️ **${TOTAL} fixable vulnerabilities (${CRIT_HIGH} Critical/High; Critical/High fail this check).**"
              echo ""
              echo "<details><summary>Full report</summary>"
              echo ""
              echo '```text'
              trivy convert --format table trivy.json
              echo '```'
              echo ""
              echo "</details>"
            fi
            echo ""
            echo "<hr>"
            echo ""
            echo "_Updated for each push; unfixable OS CVEs are excluded._"
          } > scan-comment.md
          cat scan-comment.md >> "$GITHUB_STEP_SUMMARY"
          echo "crit_high=$CRIT_HIGH" >> "$GITHUB_OUTPUT"
        id: report

      # Sticky like the preview-URL comment, but matched by the hidden marker
      # instead of `--edit-last` — both comments come from github-actions[bot],
      # so editing "the last one" would clobber whichever posted more recently.
      # Skipped for Dependabot: its pull_request runs get a read-only token,
      # so the step summary above is the only surface there.
      - name: Comment scan result (sticky, rewrites on each push)
        if: github.event_name == 'pull_request' && github.actor != 'dependabot[bot]'
        env:
          GH_TOKEN: ${{ github.token }}
          PR: ${{ github.event.pull_request.number }}
        run: |
          CID=$(gh api "repos/${GITHUB_REPOSITORY}/issues/${PR}/comments" --paginate \
            --jq '.[] | select(.body | startswith("<!-- bluebird-image-scan -->")) | .id' | head -n1)
          if [ -n "$CID" ]; then
            gh api -X PATCH "repos/${GITHUB_REPOSITORY}/issues/comments/${CID}" -F body=@scan-comment.md >/dev/null
          else
            gh api -X POST "repos/${GITHUB_REPOSITORY}/issues/${PR}/comments" -F body=@scan-comment.md >/dev/null
          fi

      # Assertions that only hold against a built image, so pytest structurally
      # cannot make them. The big one is the self-hosted Swagger UI: /docs falls
      # back to a CDN whenever `static/swagger-ui` is absent, and in a source
      # checkout it always is. Without this, retargeting the Dockerfile's COPY
      # would silently restore the third-party script surface that a strict CSP
      # (#132) depends on being gone, with every other check still green.
      - name: Smoke test the built image
        run: |
          docker run -d --rm --name smoke -p 8000:8000 bluebird:pr-scan
          for _ in $(seq 1 30); do
            curl -fsS localhost:8000/healthz >/dev/null 2>&1 && break
            sleep 1
          done

          fail=0
          check() {  # label, actual, expected
            if [ "$2" = "$3" ]; then
              echo "  ok    $1"
            else
              echo "  FAIL  $1 (want '$3', got '$2')"
              fail=1
            fi
          }

          check "build args reach /api/version" \
            "$(curl -fsS localhost:8000/api/version | jq -r .version)" "0.0.0-ci"
          check "the same version fills openapi info.version" \
            "$(curl -fsS localhost:8000/openapi.json | jq -r .info.version)" "0.0.0-ci"
          check "Swagger UI is served from the image" \
            "$(curl -s -o /dev/null -w '%{http_code}' localhost:8000/swagger-ui/swagger-ui-bundle.js)" "200"
          check "/docs references no third-party host" \
            "$(curl -fsS localhost:8000/docs | grep -cE 'jsdelivr|googleapis|tiangolo' || true)" "0"
          check "/redoc is gone" \
            "$(curl -s -o /dev/null -w '%{http_code}' localhost:8000/redoc)" "404"
          check "an unknown /api path answers as the API, not the SPA" \
            "$(curl -s -o /dev/null -w '%{content_type}' localhost:8000/api/nope)" "application/json"
          check "the SPA is still served at /" \
            "$(curl -s -o /dev/null -w '%{http_code}' localhost:8000/)" "200"

          docker stop smoke >/dev/null 2>&1 || true
          [ "$fail" -eq 0 ] || echo "::error::The built image failed its smoke test. See the failures above."
          exit $fail

      # The actual gate, after the comment so a red run still explains itself
      - name: Fail on fixable Critical/High vulnerabilities
        env:
          CRIT_HIGH: ${{ steps.report.outputs.crit_high }}
        run: |
          if [ "$CRIT_HIGH" -gt 0 ]; then
            echo "::error::${CRIT_HIGH} fixable Critical/High vulnerabilities in the image. See the scan report above."
            exit 1
          fi
