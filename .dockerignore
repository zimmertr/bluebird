.git/
.github/
.claude/
.gitignore
.dockerignore
.ruff_cache/
.DS_Store
**/*.md
docs/
examples/
LICENSE
docker-compose.yml
GitVersion.yml
frontend/node_modules/
# The API type generator runs in CI and on a developer's machine; the image
# build has no use for it and its dependency tree is large.
frontend/tools/
# The browser suite runs against the built image from outside it.
frontend/e2e/
frontend/dist/
__pycache__/
*.pyc
